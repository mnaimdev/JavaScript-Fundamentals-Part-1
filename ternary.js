// ternary or conditional operator //
const population = prompt("Enter your country population in Million ):");

const country = population > 33 ? "Portugal's population is above average" : "Portugal's population is below average";
console.log(country);
